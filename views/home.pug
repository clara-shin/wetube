extends layouts/main.pug
include mixins/videoBlock

block content
    .videos
        each item in homeVideos
            +videoBlock({
                title:item.title,
                views: item.views,
                videoFile: item.videoFile
            })